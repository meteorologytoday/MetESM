# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

MetESM couples the atmosphere model JAX-GCM (JCM), the land model Veros, and JAX-LAND through JAX-ESM (JEM).

## Status

This repository currently contains no source code — only `README.md`, `LICENSE`, and a Python `.gitignore`. There are no build, lint, or test commands yet, and no architecture to document. Update this file once the codebase is scaffolded (dependency management, module layout, test runner, etc.) so future instances have accurate, verifiable guidance instead of assumptions.
